#include <stdio.h>
#include <string.h>

#define MAX 20

int main()
{
    char str[MAX];
    int i, len;

    printf("Enter a string: ");
    scanf("%s", str);

    len = strlen(str);

    if (str[0] == '0' && str[len - 1] == '1')
        printf("Accept\n");
    else
        printf("Don't Accept\n");

    return 0;
}
