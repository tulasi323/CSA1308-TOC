#include <stdio.h>
#include <string.h>

#define MAX 20

int main()
{
    char str[MAX];
    int state = 0;
    int i, len;

    printf("Enter a string: ");
    scanf("%s", str);

    len = strlen(str);

    for(i = 0; i < len; i++)
    {
        if(state == 0)
        {
            if(str[i] == 'a')
                state = 1;
            else
                state = 3;
        }
        else if(state == 1)
        {
            if(str[i] == 'a')
                state = 1;
            else
                state = 2;
        }
        else if(state == 2)
        {
            if(str[i] == 'a')
                state = 1;
            else
                state = 2;
        }
        else
        {
            state = 3;
        }
    }

    if(state == 1)
        printf("Accept\n");
    else
        printf("Don't Accept\n");

    return 0;
}
